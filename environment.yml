name: vo2-neuristor
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy=1.24
  - scipy=1.11
  - scikit-learn=1.3
  - matplotlib=3.7
  - jupyterlab=4.0
  - ipykernel=6.25
  - pip

  - pip:
      # Any packages not on conda-forge can go here
      # e.g. a future project-specific utility package
